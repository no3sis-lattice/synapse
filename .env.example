# Noesis MCP Server Configuration
# =================================

# Path to Synapse knowledge engine tools
# This is where synapse_search.py, synapse_health.py, context_manager.py, etc. are located
# Example: /path/to/synapse/.synapse/neo4j
SYNAPSE_NEO4J_DIR=/path/to/synapse/.synapse/neo4j

# Path to Synapse Python executable (with ML dependencies)
# IMPORTANT: Must have sentence-transformers, torch, numpy, neo4j, redis installed
# This should point to the Python in Synapse's ML virtual environment (.venv-ml)
# Example: /path/to/synapse/.venv-ml/bin/python
SYNAPSE_PYTHON=/path/to/synapse/.venv-ml/bin/python

# Optional: Cache TTL in seconds (default: 300)
# CACHE_TTL=300

# Optional: Default max results for searches (default: 10)
# MAX_RESULTS_DEFAULT=10

# Optional: Enable debug logging (default: false)
# DEBUG=false
